/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com", "picsum.photos", "photos.zillowstatic.com"],
      },
};

export default nextConfig;
